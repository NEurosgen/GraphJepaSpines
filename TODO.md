Использовать окружение conda activate torch_5060

Смотри в файле run_experiment.py для запуска экспериментов. Есть checkpoint_path указывающий на уже обученнуб модель.Этот файл на данный момент щзанимается визуализацией латентного пространтсва
Я хочу чтобы ты пропустил датсет не тот который сейчас используется в коде , а основное graph_dataset из конфига через модель и сохранил все эмбеддинги в файл. Также чтобы каждый граф имел свой класс, для его определения в GraphDataset нужно передать class_path из конфига.  
После этого я хочу сделать визулизацию латентного пространства так чтобы каждый граф имел свой цвет в зависимости от класса. Для этого нужно использовать umap/tsne/pca для снижения размерности до 2D