# Graph Jepa Spines



1. Перенести маск на этап датамодуля (добавить возмодность инеграции еще одного transform) и интегрировать измененный датамодуль в модель

    1. Взять маск модуль из jepa и скопировать в normalization 
    2. Добавить новый класс обьединящий GenNorm и MaskData # для отладки полезно чтобы датасет возращал как обрезанные графы так и полный
    3. переписать датамодуль/модель лайтинг для приема нового типа обьектов
    4. Убрать из лайтинг модуля всю информацию об маскировании


2. Добавить ускорение через тензорные ядра. Также там где это нужно исправить функции на торчовые